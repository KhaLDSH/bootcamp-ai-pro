[project]
name = "csv-profiler"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "typer",
    "streamlit",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/csv_profiler"]